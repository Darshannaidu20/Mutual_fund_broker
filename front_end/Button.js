// src/components/Button.js

/**
 * Button component
 *
 * @component
 * @example
 * const label = 'Click Me'
 * return (
 *   <Button label={label} />
 * )
 */
const Button = ({ label }) => {
    return <button>{label}</button>;
  };
  
  export default Button;
  